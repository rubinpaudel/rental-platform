// Placeholder home for the scaffold PR. A later PR replaces this with a
// redirect to /dashboard once the dashboard route exists.
export default function Home() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">Huurplatform — Verhuurder</h1>
      <p className="mt-2 text-sm">
        Scaffold ready. Auth flows and dashboard land in follow-up PRs.
      </p>
    </main>
  );
}
