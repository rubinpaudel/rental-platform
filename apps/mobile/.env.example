# Public base URL of the identity API (NestJS, v1). The Better Auth Expo
# client appends /api/auth/* itself. Must be reachable from the device — on a
# physical device use your machine's LAN IP, not localhost.
EXPO_PUBLIC_API_URL=http://localhost:4000
