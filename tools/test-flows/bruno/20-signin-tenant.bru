meta {
  name: signin tenant
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/auth/sign-in/email
  body: json
  auth: none
}

body:json {
  {
    "email": "{{tenantEmail}}",
    "password": "{{password}}"
  }
}

assert {
  res.status: eq 200
}
